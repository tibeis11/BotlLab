'use server';
// ZWEI WELTEN PHASE 0: profiles.tier dropped — this file is dead code (not imported anywhere).

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function updateUserTier(_userId: string): Promise<void> {
  // noop
}
