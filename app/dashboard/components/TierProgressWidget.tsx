'use client';

// ZWEI WELTEN PHASE 0: Das alte Gamification-Reputation-System (Lehrling -> Legende)
// wurde durch tasting_iq ersetzt. Dieses Widget ist deaktiviert.
// In Phase 11 (Analytics USP Roadmap) wird ein neues TastingIQWidget gebaut.

export default function TierProgressWidget() {
  return null;
}
